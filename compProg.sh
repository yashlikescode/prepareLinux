# updating all the packages we have
sudo apt update
sudo apt upgrade

# installing g++ to run C++
sudo apt install g++

# Uncomment the below file to install python
# sudo apt get python

# installing vscode
sudo dpkg -i "vscode.deb"

# Navigating to the main folder
cd main

# Opening vscode in the main folder
code .

# Giving executable permission to runCpp
chmod +x runCpp.sh

